import { View } from 'react-native';
import { Scene } from '../../components/path-animation';

export default function PathAnimationScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#00605e' }}>
      <Scene />
    </View>
  );
}
