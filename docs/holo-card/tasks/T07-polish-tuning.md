# T07 — Polish & Tuning

**Goal:** Production-quality feel.

## Tasks

1. Tune constants in `cardLayout.ts` (rotation, parallax, spring, glare).
2. Add card drop shadow on outer wrapper (`shadow*` / `elevation`).
3. Inner border on frame layer.
4. Remove any debug overlay; keep `__DEV__` mock.

## Acceptance

Card feels like a collectible holo card with subtle depth cues.
